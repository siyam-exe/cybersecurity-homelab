# Sanitized final logrotate template for Metasploitable forwarded syslog.
# Copy to: /etc/logrotate.d/homelab-metasploitable

/var/log/metasploitable/*.log {
    weekly
    rotate 4
    compress
    missingok
    notifempty
    create 0640 syslog adm
}
